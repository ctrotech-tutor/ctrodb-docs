## Part 2 — Reactive UIs / Hero Image

**Aspect ratio:** 1200×630

**Concept:** A React component tree on the right connected to a database cylinder on the left via glowing bidirectional arrows. Code snippets of `useQuery` and `useMutation` float near the components. "Part 2 of 5" badge.

**Logo:** Top-left corner. ctrodb wordmark (user uploads the logo file separately).

**Composition:**
- Left 30%: a database cylinder icon in white (#f8fafc) on a dark slate panel (#1e293b) with rounded corners and a cyan border (#06b6d4). Below the cylinder: label "ctrodb / IndexedDB" in 12px slate-400 monospace. Subtle signal waves (concentric cyan arcs at 20% opacity) radiating from the cylinder toward the right.
- Center 15%: a thick glowing horizontal arrow (cyan #06b6d4, 8px blur) pointing left-to-right, and a thinner arrow pointing right-to-left (1px solid, 50% opacity). Small pill label between them: "useQuery / useMutation" in 11px white monospace on a dark pill (#0f172a with cyan border).
- Right 45%: three rounded rectangle components stacked vertically with angle-bracket labels. Top: `<App>` in React blue (#61dafb), Middle: `<TodoList>` in lighter blue (#7dd3fc), Bottom: `<AddTodo>` in white (#f8fafc). Each on a dark panel (#1e293b) connected by thin vertical lines suggesting a component tree. Small code label near `<TodoList>`: `useQuery("todos")` in 10px monospace cyan. Small label near `<AddTodo>`: `useMutation("todos")` in 10px monospace green.
- Bottom area spanning full width: series label "Building with ctrodb — Part 2 of 5" in slate-400 12px sans-serif, left-aligned with 60px margin

**Colors:** Background full dark slate (#0f172a), cylinder panel #1e293b, React blue #61dafb, light blue #7dd3fc, arrow cyan #06b6d4, signal waves cyan at 20%, component panels #1e293b, code labels cyan #06b6d4 and green #22c55e, series label #94a3b8.

**Typography:** Component labels in 14px monospace, code pills in 10px monospace, series label in 12px sans-serif. No serif fonts.

**Technical notes:** 1200×630. Logo top-left (user uploads separately). The signal waves should be subtle — faint concentric arcs, not a loud pattern. The arrow glow should be a soft blur, not a neon tube. Components should look like a tree diagram, not a screenshot.
