## X Post 7 — Zero Dependencies

**Aspect ratio:** 1200×675

**Concept:** A minimalist "2,400 LOC / 0 deps" stat card. A small TypeScript logo, a small database icon, and the npm install command.

**Composition:**
- Center: "2,400 lines" in large bold text
- Below: "zero dependencies" in smaller text
- Bottom: "npm install ctrodb" in a code-style pill
- Top corners: TypeScript logo (left), database icon (right)
- Background: clean white or very light slate

**Style:** Minimal stat card. Think "npm" package card. Clean, scannable.

**Colors:** Background white (#f8fafc), number text in slate (#0f172a), subtitle in gray (#64748b), code pill background slate (#1e293b) with white text. TypeScript blue (#3178c6).

**Technical notes:** Export as PNG. This is the most text-heavy card — ensure all text is legible at X card display size (usually ~440px wide).
