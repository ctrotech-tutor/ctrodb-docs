## Medium Post 3 — OG Card

**Aspect ratio:** 1200×630

**Concept:** A magnifying glass floating over a browser window. Inside the browser, rows of text documents are visible. The magnifying glass highlights one document. Represents full-text search running in the browser.

**Logo:** Bottom-right corner. ctrodb wordmark (user uploads the logo file separately).

**Composition:**
- Center: a browser window mockup (dark rounded rectangle with a thin chrome outline). Inside the browser: 4-5 horizontal lines representing document rows. The third row is highlighted in cyan (#06b6d4) with a glow.
- Over the browser: a magnifying glass icon, slightly tilted, its lens overlapping the highlighted row. The glass handle points to the upper-right.
- Top-left area: headline "Full-Text Search in the Browser" in white serif, 40pt. Below it, subhead: "No Server Required" in cyan, 22pt italic.
- Far background: subtle grid pattern (developer aesthetic) in very low opacity (5%) — just enough to suggest a code editor background

**Colors:** Background dark navy (#0f172a), browser interior dark gray (#1e293b), highlight cyan (#06b6d4), text white (#f8fafc), magnifying glass handle slate-400 (#94a3b8).

**Typography:** Headline serif, subhead italic sans-serif. Magnifying glass icon should be a clean line-art style, not emoji.

**Technical notes:** 1200×630. Logo bottom-right (user uploads separately). The magnifying glass should look like a UI icon, not a hand-drawn illustration. The highlighted row should glow subtly — not brightly, just enough to draw the eye.
