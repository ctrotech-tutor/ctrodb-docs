## Medium Post 4 — OG Card

**Aspect ratio:** 1200×630

**Concept:** A two-column comparison. Left column: traditional server architecture (browser → server → database → response). Right column: client-side architecture (browser with embedded database). The right side is simpler, with fewer arrows. The visual makes the argument without words.

**Logo:** Top-left corner. ctrodb wordmark (user uploads the logo file separately).

**Composition:**
- Left 50%: three boxes connected by two arrows: [Browser] → [Server] → [Database]. Boxes are rounded rectangles in muted gray (#475569). Arrows in warm gray (#94a3b8). Small "X" or strikethrough overlay in red (#ef4444, 30% opacity) suggesting this path is overly complex.
- Right 50%: two boxes connected by one arrow: [Browser] ← Database icon →. The browser box is white with a cyan border. The database icon (cylinder) is inside the browser box — showing it's embedded. Arrow in cyan (#06b6d4). Clean and minimal.
- Top area above the architectures: headline "Client-Side Databases Are Underrated" in white serif, 38pt, centered across the full width
- Bottom edge: subtle horizontal line in cyan, 50% width centered

**Colors:** Background dark slate (#0f172a), left architecture muted gray (#475569), right architecture cyan (#06b6d4), text white (#f8fafc), delete overlay red (#ef4444 at 30% opacity).

**Typography:** Headline serif, 38pt centered. Architecture labels in 14pt monospace.

**Technical notes:** 1200×630. Logo top-left (user uploads separately). The red strikethrough should be subtle — it's not "old bad, new good" aggression, just "simpler path is available." The database icon should be the standard cylinder shape, not a custom symbol.
