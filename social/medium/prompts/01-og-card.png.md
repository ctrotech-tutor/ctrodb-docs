## Medium Post 1 — OG Card

**Aspect ratio:** 1200×630 (Medium header standard)

**Concept:** A browser window on the left, database cylinder icon on the right, with a subtle data-flow arrow connecting them. The visual says "this database runs inside your browser."

**Logo:** Top-left corner. ctrodb wordmark (user uploads the logo file separately and places it here).

**Composition:**
- Left 50%: simplified browser window chrome (tabs, URL bar, minimal UI) with a glowing cyan database icon visible inside the window
- Right 50%: headline "A Database That Lives in Your Browser" set in a clean serif (like Playfair Display or Literata), 40-48pt, left-aligned
- Bottom-left below headline: "ctrodb — Zero-dependency client-side database" in 16pt sans-serif
- Background: dark slate gradient (#0f172a → #1e293b) — the browser window and text sit on this dark base
- The arrow connecting browser to database is a subtle horizontal line with a chevron, in cyan (#06b6d4), 50% opacity

**Typography:**
- Headline: serif, white (#f8fafc), 42pt
- Subtitle: sans-serif, slate-300 (#cbd5e1), 16pt
- No decorative elements — flat, minimal

**Technical notes:** Export at 1200×630. Leave 80px padding on all sides. The logo goes in the top-left corner — the designer places the uploaded .png/.svg there, not the AI.
