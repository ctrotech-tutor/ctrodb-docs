## Build a Notes App with ctrodb / Hero OG Card

**Aspect ratio:** 1200×630

**Concept:** A realistic browser window mockup showing the completed notes app in dark theme. Multiple note cards visible, one pinned with a yellow border, search bar at the top, a "+" button in the corner. Looks like a Dev tutorial screenshot.

**Logo:** Top-left corner, outside the browser window. ctrodb wordmark (user uploads the logo file separately).

**Composition:**
- Full background: a soft dark gradient (#0f172a at top → #1e293b at bottom). In the background, very faint grid lines at 5% opacity.
- Center: a browser window mockup (1200×630 canvas, the window is about 900×480, centered). The window has a thin dark gray chrome (#334155) with traffic light dots (red, yellow, green) in the top-left corner of the chrome.
- Inside the browser window (content area at #0f172a):
  - Top bar: search input (rounded, dark bg #1e293b, cyan border on focus, placeholder "Search notes..." in slate-500)
  - Below search: grid of 6 note cards (3 columns × 2 rows). Each card is #1e293b with rounded corners (#1e293b fill, #334155 1px border).
  - Card 1 (top-left) is pinned: yellow border (#eab308), small pin icon top-right, title "Meeting Notes" bold, body preview in dim text, date at bottom
  - Other cards have different lengths (some tall text, some short) to look realistic
  - Card 4 has a cyan tag/badge "ctrodb" on it
  - Bottom-right floating "+" button in cyan (#06b6d4) with white "+" text
- Overlay at bottom of the browser chrome: banner with "Built with HTML + Tailwind + ctrodb" in small text, with a small database icon before "ctrodb"
- Above the browser window, top-left area: headline "Build a Notes App with ctrodb" in white sans-serif, 30pt, outside the browser

**Colors:** Page background #0f172a, browser chrome #334155, window content #0f172a, cards #1e293b, card borders #334155, pinned border yellow #eab308, accent cyan #06b6d4, search focus border #06b6d4, button #06b6d4, text white #f8fafc.

**Typography:** All sans-serif. Headline Inter bold 30pt. Notes card titles Inter medium 14pt. Body text Inter regular 12pt in slate-400. Search text 14px. The "Built with" banner in 11px sans-serif.

**Technical notes:** 1200×630. Logo top-left (user uploads separately). The browser window should be recognizable as a browser but simplified — no URL bar details, just the chrome outline and traffic light dots. The notes UI should look functional but not production-polished — it's a tutorial demo app.
