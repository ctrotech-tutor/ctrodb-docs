## Full-Text Search in the Browser / Hero OG Card

**Aspect ratio:** 1200×630

**Concept:** Search pipeline as a flow diagram. Documents enter on the left, pass through an inverted index (visualized as a tree or a table), and produce ranked results on the right. Dark background, technical diagram aesthetic.

**Logo:** Bottom-left corner. ctrodb wordmark (user uploads the logo file separately).

**Composition:**
- Left 25%: 3-4 document rectangles stacked vertically, each with horizontal lines suggesting text content. Soft amber (#f59e0b at 40% opacity) fill, thin borders.
- Center 40%: an inverted index table or tree structure. If a table: two columns — "Token" and "Documents" — with visible rows like "react → [1,3,7]", "hooks → [7,12]", "typescript → [1,5]". Column headers in white bold, data rows in cyan (#06b6d4). The table has a subtle cyan border and sits on a slightly lighter panel (#1e293b).
- Right 25%: 3 ranked result rectangles. The top result is highlighted in cyan with a glow. Results have a small rank number (#06b6d4) on the left.
- Arrows between sections: thin cyan arrows (#06b6d4), pointing from left to right, with a small "tokenize" label under the first arrow and "intersect" under the second.
- Top area: headline "Full-Text Search in the Browser" in white serif, 36pt, left-aligned. Below it: "No Server Required" in cyan italic, 20pt.

**Colors:** Background #0f172a, document panels #f59e0b at 40%, index panel #1e293b, index text cyan #06b6d4, results white #f8fafc with cyan highlight, arrows #06b6d4, headline white (#f8fafc).

**Typography:** Headline serif (Playfair Display or Literata) 36pt, subhead italic sans-serif 20pt. Index table in 13pt monospace (JetBrains Mono). Labels in 11pt sans-serif.

**Technical notes:** 1200×630. Logo bottom-left (user uploads separately). The inverted index table should look like a real data structure — column alignment, clean rows, monospace font feel. Don't make it look like a screenshot, make it look like a technical illustration.
