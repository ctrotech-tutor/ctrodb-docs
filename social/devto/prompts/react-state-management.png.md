## React State Management Without Redux / Hero OG Card

**Aspect ratio:** 1200×630

**Concept:** A React component tree on the left (three boxes with <> labels), a database cylinder on the right, and a glowing bidirectional signal arrow between them. The visual says "React ⬄ database, no middleware."

**Logo:** Top-left corner. ctrodb wordmark (user uploads the logo file separately).

**Composition:**
- Left 45%: three rounded rectangles stacked vertically, each with angle-bracket labels suggesting React components. Top: `<App>` in React blue (#61dafb). Middle: `<TodoList>` in slightly lighter blue (#7dd3fc). Bottom: `<AddTodo>` in white (#f8fafc). Each sits on a dark panel (#1e293b) with thin borders. The components are connected by subtle vertical lines simulating a component tree.
- Center: a glowing horizontal arrow (cyan, #06b6d4, with a soft 8px blur behind it) pointing from the component tree to the database and back. Label above the arrow: "useQuery / useMutation" in 13px white monospace on a small dark pill (#0f172a).
- Right 30%: a database cylinder icon in cyan (#06b6d4) on a slightly elevated panel. Below the cylinder: label "IndexedDB" in 14px slate-300. The cylinder is the standard 2D database symbol (rectangle with oval top/bottom), not a 3D render.
- Bottom area spanning full width: headline "React State Management Without Redux" in white sans-serif, 34pt, left-aligned with 60px margin

**Colors:** Background #0f172a (full bleed), React blue #61dafb, light blue #7dd3fc, component panel #1e293b, database panel #1e293b, arrow cyan #06b6d4, headline white #f8fafc, label text #cbd5e1.

**Typography:** Headline bold sans-serif (Inter) 34pt. Component labels in 14px monospace. Arrow label in 13px monospace. No serif anywhere.

**Technical notes:** 1200×630. Logo top-left (user uploads separately). The glow on the arrow should be subtle — a soft cyan blur, not a neon tube. The component tree should look like a diagram, not a screenshot of actual code.
