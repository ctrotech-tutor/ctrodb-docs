## Client-Side Databases Are Underrated / Hero OG Card

**Aspect ratio:** 1200×630

**Concept:** Split comparison. Left side shows the traditional architecture (browser → server → database → response) in muted red. Right side shows the client-side model (browser with embedded database, single arrow to UI) in green. A "vs" in the center.

**Logo:** Top-left corner. ctrodb wordmark (user uploads the logo file separately and places it here).

**Composition:**
- Left 48%: three rounded rectangles in warm gray (#475569) connected by two arrows. Stack: [Browser] → [Server] → [Database]. Below the stack: label "200-500ms per round-trip" in 14px red-300 (#fca5a5). Everything on the left has a very faint red overlay tint (5% opacity).
- Center: a subtle vertical divider line in warm gray (#94a3b8), 60px tall, centered. "vs" in 14px uppercase, slate-400, centered on the divider.
- Right 48%: a single browser rectangle (white bg, #f8fafc) containing a small database cylinder icon inside it. One arrow from the browser pointing down to a UI mockup (simple rectangle with 3 horizontal lines suggesting a list view). Below: label "1ms, no network" in 14px green-400 (#4ade80). Right side has a faint green tint overlay.
- Top area spanning both halves: headline "Client-Side Databases Are Underrated" in white sans-serif, 36pt, left-aligned with 60px margin

**Colors:** Background dark slate (#0f172a), left boxes #475569, right boxes white (#f8fafc), red text #fca5a5, green text #4ade80, divider #94a3b8, headline white (#f8fafc).

**Typography:** Headline Inter bold 36pt, labels Inter regular 14pt, "vs" Inter uppercase 14pt. Architecture labels in 12pt monospace (JetBrains Mono or Fira Code).

**Technical notes:** 1200×630. Logo top-left (user uploads separately). The red/green tint should be subtle — not aggressive, just enough to visually differentiate the two sides. The browser rectangle on the right should look like a simplified browser window (thin top bar, content area).
